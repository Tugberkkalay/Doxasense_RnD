# Backend API URL
REACT_APP_BACKEND_URL=https://demobackend.emergentagent.com

# Disable host check for preview
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Disable WebSocket (use polling instead for preview compatibility)
WDS_SOCKET_HOST=0.0.0.0
FAST_REFRESH=false
